import { Routes } from '@angular/router';
import { ComputadoraComponent } from './computadora/computadora.component';

export const routes: Routes = [
  { path: 'computadora', component: ComputadoraComponent }
];
